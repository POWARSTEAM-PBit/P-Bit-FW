#pragma once

// Temporary lab dashboard screen used to test layout, icons, and compact
// sensor summaries without touching the production screens.
void draw_lab_dash_screen(bool screen_changed, bool sensor_data_changed);

